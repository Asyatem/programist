#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS
//#include <>
int main() {
	int a, b, S, P;
	scanf_s("%d", &a);
	scanf_s("%d", &b);
	S = a * b;
	P = (a + b)*2;
	printf("S=%d\n P=%d\n", S, P);

	return 0;
}
